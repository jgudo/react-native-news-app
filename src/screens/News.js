import React from 'react';
import { ScrollView, View, Text } from 'react-native';

const News = () => {
  return (
    <ScrollView>
      <View>
        <Text>Home</Text>
      </View>
    </ScrollView>
  );
};

export default News;
